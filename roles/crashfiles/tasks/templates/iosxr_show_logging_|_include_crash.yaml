# fmt: off
[
  {
    "name": "crash_lines",
    "getval": "(?m)^(?P<line>.*crash.*)$",
    "result": {
      "crash_files": [
        {
          "file": "{{ line }}"
        }
      ]
    }
  }
]
# fmt: on